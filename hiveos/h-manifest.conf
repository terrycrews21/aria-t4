# ARIAMiner — HiveOS custom miner manifest
CUSTOM_NAME=ariaminer
CUSTOM_VERSION=0.2.0
CUSTOM_BUILD=1
# Port of ariaminer's --stats-port JSON endpoint (h-stats.sh reads it).
CUSTOM_API_PORT=4068
CUSTOM_ALGO=gemm-int7
